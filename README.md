# AI Resume Screener

Match resumes (PDF) with a job description using NLP and similarity scoring.

## Setup
1. Clone or download the project.
2. Install dependencies:
   ```bash
   pip install -r requirements.txt